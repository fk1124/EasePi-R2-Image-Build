# FNOS Rootfs

Reserved for the FNOS stable target.

The project matrix tracks FNOS separately because it uses an FN-specific kernel
path and should not be forced through the normal Debian/Ubuntu kernel aliases.
