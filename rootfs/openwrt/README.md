# OpenWrt

Reserved for OpenWrt matrix targets.

Planned releases:

- `24`: OpenWrt 24, kernel 6.6
- `25`: OpenWrt 25, kernel 6.12

OpenWrt should be integrated as its own image path rather than sharing the
Debian/Ubuntu BSP rootfs stages.
