# Alpine Linux Rootfs

Reserved for the Alpine Linux matrix target.

Expected direction:

- `apk` based rootfs bootstrap
- lightweight server profile first
- kernel/BSP reuse from the project kernel adapters where possible
