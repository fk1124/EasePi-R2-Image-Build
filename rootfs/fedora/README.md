# Fedora Rootfs

Reserved for the Fedora matrix target.

Expected direction:

- Fedora latest rootfs, currently tracked as Fedora 44 in the project matrix
- package profile split after the Debian/Ubuntu minimal/server/desktop model
- distro-specific boot and network defaults kept in this directory
